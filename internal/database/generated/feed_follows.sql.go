// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: feed_follows.sql

package database

import (
	"context"
	"database/sql"

	"github.com/google/uuid"
)

const createFeedFollow = `-- name: CreateFeedFollow :one
WITH inserted AS (
    INSERT INTO feeds_follows (user_id, feed_id)
    VALUES ($1, $2)
    RETURNING id, created_at, updated_at, user_id, feed_id
)
SELECT inserted.id, inserted.created_at, inserted.updated_at, inserted.user_id, inserted.feed_id, users.name AS user_name, feed.name AS feed_name
FROM inserted
JOIN users ON inserted.user_id = users.id
JOIN feed ON inserted.feed_id = feed.id
`

type CreateFeedFollowParams struct {
	UserID uuid.UUID
	FeedID uuid.UUID
}

type CreateFeedFollowRow struct {
	ID        uuid.UUID
	CreatedAt sql.NullTime
	UpdatedAt sql.NullTime
	UserID    uuid.UUID
	FeedID    uuid.UUID
	UserName  string
	FeedName  string
}

func (q *Queries) CreateFeedFollow(ctx context.Context, arg CreateFeedFollowParams) (CreateFeedFollowRow, error) {
	row := q.db.QueryRowContext(ctx, createFeedFollow, arg.UserID, arg.FeedID)
	var i CreateFeedFollowRow
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.UserID,
		&i.FeedID,
		&i.UserName,
		&i.FeedName,
	)
	return i, err
}

const deleteFeedFollow = `-- name: DeleteFeedFollow :exec
DELETE FROM feeds_follows
USING feed
WHERE feeds_follows.feed_id = feed.id
  AND feeds_follows.user_id = $1
  AND feed.url = $2
`

type DeleteFeedFollowParams struct {
	UserID uuid.UUID
	Url    string
}

func (q *Queries) DeleteFeedFollow(ctx context.Context, arg DeleteFeedFollowParams) error {
	_, err := q.db.ExecContext(ctx, deleteFeedFollow, arg.UserID, arg.Url)
	return err
}

const getFollowedFeedsNames = `-- name: GetFollowedFeedsNames :many
SELECT feed.name, feed.url
FROM feeds_follows
JOIN feed ON feeds_follows.feed_id = feed.id
WHERE feeds_follows.user_id = $1
`

type GetFollowedFeedsNamesRow struct {
	Name string
	Url  string
}

func (q *Queries) GetFollowedFeedsNames(ctx context.Context, userID uuid.UUID) ([]GetFollowedFeedsNamesRow, error) {
	rows, err := q.db.QueryContext(ctx, getFollowedFeedsNames, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetFollowedFeedsNamesRow
	for rows.Next() {
		var i GetFollowedFeedsNamesRow
		if err := rows.Scan(&i.Name, &i.Url); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
